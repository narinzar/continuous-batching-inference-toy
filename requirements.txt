# Install torch first from the CUDA 12.8 wheel index (RTX 5090 / sm_120):
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
# Then: pip install -r requirements.txt
torch
transformers
fastapi
uvicorn
numpy
tqdm
pytest
pytest-asyncio
python-dotenv
httpx
