# data/

This project does not need any input dataset. Prompts for the benchmark are
generated on the fly in `scripts/run_bench.py`.

If you want to benchmark against your own prompts, drop a plain text file here
(one prompt per line) and adapt `run_bench.py` to read it. Anything you add in
this folder is gitignored except this README.
