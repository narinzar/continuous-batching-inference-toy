# No secrets are required to run this project.
# The default model "sshleifer/tiny-gpt2" is public and needs no token.
#
# This placeholder exists only so the loader has a file to read.
APP_ENV=local

# Optional: only needed if you swap in a gated Hugging Face model.
# HF_TOKEN=your_hf_token_here
